#Got Tweet?

##Sample app for:
* get tweet by ID
* store tweet
* show tweet

##Setup
  git clone git@github.com:xta/GetTweet.git
  bundle
  cd GetTweet
setup `/config/config.yml` with Twitter keys

  rake db:migrate
  rails server