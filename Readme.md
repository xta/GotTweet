#Got Tweet?

##Sample app for:
* get tweet by ID
* store tweet
* show tweet

##Setup

    git clone git@github.com:xta/GotTweet.git
    bundle
    cd GotTweet
setup `/config/config.yml` with Twitter keys

    rake db:migrate
    rails server
